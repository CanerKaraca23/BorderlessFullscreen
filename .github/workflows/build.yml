name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v6
      with:
        submodules: recursive
    
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2
    
    - name: Generate project files
      run: .\premake5.bat
    
    - name: Build project
      run: msbuild build\BorderlessFullscreen.sln /p:Configuration=Release /p:Platform=Win32 /m /verbosity:minimal
    
    - name: Upload artifact
      uses: actions/upload-artifact@v6
      with:
        name: BorderlessFullscreen
        path: data\BorderlessFullscreen.asi
